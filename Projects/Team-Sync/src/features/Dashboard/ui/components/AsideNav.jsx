import React from "react";

const AsideNav = () => {
  return (
    <div className="">
      <h1 className="text-3xl font-semibold text-[var(--text-primary)]">
        team-sync
      </h1>
      <div className="text-[var(--text-muted)]">Enterprise workspace</div>
    </div>
  );
};

export default AsideNav;
